"""Built-in connectors discovered automatically by the registry.

Each module here contributes one or more :class:`BaseConnector` subclasses.
Adding a new source is as simple as dropping a new module in this package (or in
``threat_hunting.plugins``) — no core or pipeline change is required.
"""
